# 2026-03-21

## 今日執行事項
- 12:14 Taipei heartbeat：檢查今日定時 deliverables；08:00 OpenClaw daily brief 與 08:30 市場快報皆逾期且尚未 ack。
- 12:14 Taipei heartbeat：直接續跑補件流程，抓取 OpenClaw 官方 releases/docs、ClawHub 首頁資訊、X 搜尋結果；Brave Search 對部分查詢回傳 429 rate limit。
- 12:14 Taipei heartbeat：準備向 Rong 送出合併逾期快報，並註記市場資料部分來源受限。
- 07:32 UTC：診斷 Google Vertex / Gemini 路由；確認 `vertex-proxy` 可直接回應，問題較像 provider 命名/路由混用，不是 Vertex 掛掉。
- 07:34 UTC：依 Rong 指示清理 `/home/node/.openclaw/openclaw.json` 的 model config，只保留 `vertex-proxy/*` 模型項，移除 `google-vertex/*` 與其他非 proxy 模型註冊。

## 任務狀態
- OpenClaw daily brief（08:00）：pending → sent-awaiting-ack（本次 heartbeat 補送）
- 市場快報（08:30）：pending → sent-awaiting-ack（本次 heartbeat 補送；Reddit/X 搜尋部分受 rate limit 限制，已明講缺口）
- Model config clean-up：done（已改檔；`openclaw.json` 不在 workspace git repo，無法用 workspace repo 提交）

## Blockers
- Brave Search Free plan 出現 429，導致 Reddit / 額外官方搜尋無法穩定抓取。
- 本機缺少 rg，已改用 grep 作為備援。
- `/home/node/.openclaw/openclaw.json` 位於 workspace repo 外；本次變更無法依 workspace git 流程 commit。

## 觀察與學習
- 心跳該直接補件，不該只提醒「還沒做」。
- 市場快報若要穩定，得建立固定來源池與 rate-limit 備援。
- OpenClaw 模型表面名稱相近，但 `google-vertex/*` 與 `vertex-proxy/*` 是不同路徑，混用很容易出鬼。

## 後續審查
- 13:14 Taipei heartbeat：重新審查今日 deliverables；08:00 日報與 08:30 市場快報仍為 sent-awaiting-ack，距上次提醒未滿 4 小時，且無新 blocker / 新進度 / 新 deadline，因此本輪不重複提醒。
